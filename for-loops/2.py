# This Python program uses a for loop to print the elements of a list.

# Create a list of items.
items = [1, 2, 3, 4, 5]

# Create a for loop that iterates over the items in the list.
for item in items:

    # Print the current item.
    print(item)